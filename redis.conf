# Restrict Redis memory consumption (set to 512MB for a 4GB server ecosystem)
maxmemory 512mb

# Eviction strategy: Remove the Least Recently Used keys automatically when full
maxmemory-policy allkeys-lru